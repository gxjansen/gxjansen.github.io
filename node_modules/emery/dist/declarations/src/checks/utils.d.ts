import { UnaryPredicate } from "../types.js";
/**
 * Returns a new function for checking *all* cases against a value, a bit
 * like `pipe` for predicates.
 */
export declare function checkAll<T>(...predicates: UnaryPredicate<T>[]): (value: T) => boolean;
/** Apply *all* checks against a value. */
export declare function checkAllWith<T>(value: T, ...predicates: UnaryPredicate<T>[]): boolean;
/** Returns a new negated version of the stated predicate function. */
export declare function negate<T>(predicate: UnaryPredicate<T>): (value: T) => boolean;
//# sourceMappingURL=utils.d.ts.map