import { i as integer } from './index-4fcf9271.js';
import { aY as text } from './index-cf1193f7.js';

const cloudImageSchema = {
  src: text({
    label: 'URL',
    validation: {
      length: {
        min: 1
      }
    }
  }),
  alt: text({
    label: 'Alt text'
  }),
  height: integer({
    label: 'Height'
  }),
  width: integer({
    label: 'Width'
  })
};

export { cloudImageSchema as c };
