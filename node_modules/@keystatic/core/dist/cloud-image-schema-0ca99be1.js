import { i as integer } from './index-65b27f1d.js';
import { b0 as text } from './index-330df5ae.js';

const cloudImageSchema = {
  src: text({
    label: 'URL',
    validation: {
      length: {
        min: 1
      }
    }
  }),
  alt: text({
    label: 'Alt text'
  }),
  height: integer({
    label: 'Height'
  }),
  width: integer({
    label: 'Width'
  })
};

export { cloudImageSchema as c };
