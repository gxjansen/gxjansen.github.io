import { type Doc } from 'yjs';
import type { DocDataSource } from './data-source';
import type { DataSourceAdapter } from './types';
interface LazyProviderOptions {
    origin?: string;
}
export type DocProvider = {
    passive: true;
    sync(onlyRootDoc?: boolean): Promise<void>;
    get connected(): boolean;
    connect(): void;
    disconnect(): void;
};
/**
 * Creates a lazy provider that connects to a datasource and synchronizes a root document.
 */
export declare const createLazyProvider: (rootDoc: Doc, datasource: DocDataSource, options?: LazyProviderOptions) => DocProvider & DataSourceAdapter;
export {};
//# sourceMappingURL=lazy-provider.d.ts.map