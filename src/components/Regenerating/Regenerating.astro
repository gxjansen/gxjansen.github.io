---
// Animated mana regeneration bar for sleep transition
---

<div class="regenerating">
  <div class="regenerating-text">REGENERATING...</div>
  <div class="regenerating-bar">
    <span class="bar-empty">░░░░░░░░░░</span>
    <span class="bar-fill">██████████</span>
  </div>
</div>

<style>
  .regenerating {
    background-color: #000;
    padding: 3em 2em;
    margin: 2em 0;
    text-align: center;
    font-family: monospace;
  }

  .regenerating-text {
    color: #666;
    margin-bottom: 1em;
    animation: text-pulse 3s ease-in-out infinite;
  }

  .regenerating-bar {
    position: relative;
    display: inline-block;
    letter-spacing: 0.1em;
  }

  .bar-empty {
    color: #333;
  }

  .bar-fill {
    position: absolute;
    left: 0;
    top: 0;
    color: #4a9;
    clip-path: inset(0 100% 0 0);
    animation: fill-bar 24s ease-in-out infinite;
  }

  @keyframes fill-bar {
    0% {
      clip-path: inset(0 100% 0 0);
    }
    90% {
      clip-path: inset(0 0 0 0);
    }
    100% {
      clip-path: inset(0 0 0 0);
    }
  }

  @keyframes text-pulse {
    0%, 100% {
      opacity: 0.4;
    }
    50% {
      opacity: 0.7;
    }
  }
</style>
