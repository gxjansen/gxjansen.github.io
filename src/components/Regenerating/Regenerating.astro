---
// Animated mana regeneration bar for sleep transition
---

<div class="regenerating">
  <div class="regenerating-text">REGENERATING...</div>
  <div class="regenerating-bar-container">
    <span class="bar-empty">░░░░░░░░░░░░░░░</span>
    <span class="bar-fill">███████████████</span>
    <span class="bar-percent"></span>
  </div>
</div>

<style>
  .regenerating {
    @apply bg-base-950;
    padding: 3em 2em;
    margin: 2em 0;
    text-align: center;
    font-family: monospace;
  }

  .regenerating-text {
    @apply text-base-500;
    margin-bottom: 1em;
    animation: text-pulse 3s ease-in-out infinite;
  }

  .regenerating-bar-container {
    position: relative;
    display: inline-flex;
    align-items: center;
    gap: 0.75em;
    font-size: 1.25em;
  }

  .bar-empty {
    @apply text-base-600;
  }

  .bar-fill {
    position: absolute;
    left: 0;
    top: 0;
    @apply text-foam;
    clip-path: inset(0 100% 0 0);
    animation: fill-bar 8s ease-in-out infinite;
  }

  .bar-percent {
    @apply text-foam;
    min-width: 3em;
    text-align: right;
  }

  .bar-percent::after {
    content: "0%";
    animation: count-percent 8s ease-in-out infinite;
  }

  @keyframes fill-bar {
    0% {
      clip-path: inset(0 100% 0 0);
    }
    90% {
      clip-path: inset(0 0 0 0);
    }
    100% {
      clip-path: inset(0 0 0 0);
    }
  }

  @keyframes count-percent {
    0% {
      content: "0%";
    }
    10% {
      content: "12%";
    }
    20% {
      content: "26%";
    }
    30% {
      content: "38%";
    }
    40% {
      content: "52%";
    }
    50% {
      content: "64%";
    }
    60% {
      content: "75%";
    }
    70% {
      content: "84%";
    }
    80% {
      content: "92%";
    }
    90%,
    100% {
      content: "100%";
    }
  }

  @keyframes text-pulse {
    0%,
    100% {
      opacity: 0.4;
    }
    50% {
      opacity: 0.7;
    }
  }
</style>
